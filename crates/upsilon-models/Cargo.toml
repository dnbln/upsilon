[package]
name = "upsilon-models"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1"
rust-argon2 = "1"
bcrypt = "0.13"
juniper = "0.15.10"

upsilon-core = { path = "../upsilon-core" }
upsilon-id = { path = "../upsilon-id" }
upsilon-stdx = { path = "../upsilon-stdx" }